<div class="container">
  <div class="page-header">
    <h1>Indexed Pages for <%= @ticker %></h1>
  </div>

  <ol>
    <% @pages.each do |page| %>
      <li><a href="<%= page %>" target="top"><%= page %></a></li>
    <% end %>
  </ul>
</div>  